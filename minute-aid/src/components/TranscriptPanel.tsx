import React, {ReactElement} from 'react';

export interface TranscriptPanelProps {
  transcript: string;
}

export const TranscriptPanel: React.FC<TranscriptPanelProps> = (props: TranscriptPanelProps) => {
  return <div></div>
}